{
  "scripts": {
    "dev": "concurrently \"cd client && yarn dev\" \"cd server && yarn dev\"",
    "setup": "concurrently \"cd client && yarn install\" \"cd server && yarn install\""
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
